import { mergeTwoLists } from "./utils"

describe('Utils', () => {
    describe('merge 2 sorted linked list', () => {
        it('should merge 1,2,4 and 1,3,4 to 1,1,2,3,4,4', () => {
            const nodeOne = SinglyLinkedListNode()
            mergeTwoLists()
        })
    })
})